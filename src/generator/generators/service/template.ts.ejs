import { Injectable, Inject } from '@nestjs/common';
<%- importModules %>

@Injectable()
export class <%= pascalName %>Service {
  constructor(
    @Inject(<%= tableName.toUpperCase() %>_REPOSITORY)
    private readonly <%= camelName %>Model: typeof <%= pascalName %>Model,
  ) {
  }
}